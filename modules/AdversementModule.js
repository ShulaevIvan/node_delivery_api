const adversmentCollection = require('../models/Adversement');

class AdversmentModule {
    constructor() {
        this.adversmentCollection = adversmentCollection;
    }
    
    async create(data) {
        try {
            return new Promise((resolve, reject) => {

            });
        }
        catch(err) {

        }
    }
};